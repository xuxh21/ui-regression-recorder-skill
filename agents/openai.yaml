interface:
  display_name: "UI Regression Recorder"
  short_description: "Plan, split, compose, and run reusable UI regression flows"
  default_prompt: "Use this skill to initialize UI regression, decompose full flows into reusable capabilities, and assemble new flows from existing helpers or the current Chrome page."
