interface:
  display_name: "UI Regression Recorder"
  short_description: "Initialize, record, clean, and run UI regression flows"
  default_prompt: "Use this skill to initialize this project for UI regression, record operations, extract shared helpers, and run named flows safely."
